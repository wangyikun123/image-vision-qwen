---
name: codex-image-vision-kimi
description: ? Nvidia NIM ? Kimi K2.6 (moonshotai/kimi-k2.6) ??????????????????base64 ????????/??/??????/??/OCR ???????????????????????????? Nvidia NIM API?OpenAI ?????????????? Codex ????????? NVIDIA_API_KEY ????? cc-switch.db ??? key?
---

# ???? via Nvidia NIM (Kimi K2.6)

## ?????????

**????????????** ???????**?? PowerShell ?? Nvidia NIM** ??????
- ?? cc-switch??? env??????
- ?????? Codex ????????GLM / DeepSeek / ???
- Nvidia NIM ? OpenAI ?????????????? max_tokens ??

?????????????????????????????????????????????????????

## ????

????????
- ????**??????**?`.png` `.jpg` `.jpeg` `.gif` `.webp` `.bmp`?
- ??**?? base64 ????**
- ???????????????????????????????OCR?????????????????/??/???
- ????????UI ???????????????

**????**???????????git ????????????????????

## ?????API Key

? PowerShell ?? Nvidia API key?**?????? `NVIDIA_API_KEY`**????? `cc-switch.db` ?????

```powershell
# ?? 1: ????????
$NVIDIA_KEY = $env:NVIDIA_API_KEY

# ?? 2: ? cc-switch.db ?????? base URL ? nvidia ? provider?
if (-not $NVIDIA_KEY) {
    $NVIDIA_KEY = python -c @"
import sqlite3, json
c = sqlite3.connect(r'C:\Users\hao18\.cc-switch\cc-switch.db')
for row in c.execute("SELECT settings_config FROM providers WHERE app_type='claude'").fetchall():
    if not row[0]: continue
    cfg = json.loads(row[0]); env = cfg.get('env', {})
    if 'nvidia' in (env.get('ANTHROPIC_BASE_URL') or '').lower():
        print(env.get('ANTHROPIC_AUTH_TOKEN', '')); break
"@
}

if (-not $NVIDIA_KEY) {
    Write-Error "??? NVIDIA API key???? `$env:NVIDIA_API_KEY ?? cc-switch ??? Nvidia provider?"
    exit 1
}
```

> **???????** ? `$env:VISION_MODEL` ?????????? `moonshotai/kimi-k2.6`?Nvidia NIM ???????????? `qwen/qwen3-vl-plus`?`meta/llama-4-maverick` ????? [NVIDIA NIM ??](https://build.nvidia.com/explore/discover) ???

## ????

### 1. ???? ? base64

????? base64 ?????????????????????

```powershell
$IMG_PATH = "C:\path\to\image.png"  # ??????

# ?? media_type
$MT = switch -Wildcard ($IMG_PATH) {
    '*.png'  { 'image/png' }
    '*.jpg'  { 'image/jpeg' }
    '*.jpeg' { 'image/jpeg' }
    '*.gif'  { 'image/gif' }
    '*.webp' { 'image/webp' }
    '*.bmp'  { 'image/bmp' }
    default  { 'image/png' }
}

# ? base64?Windows ? base64 ?????
$B64 = [Convert]::ToBase64String([IO.File]::ReadAllBytes($IMG_PATH))
$B64_PATH = "$env:TEMP\img.b64"
[IO.File]::WriteAllText($B64_PATH, $B64)
```

**???????? base64**?????????????????????? `data:image/...;base64,` ?????????

### 2. ???????? Nvidia NIM

?? Nvidia NIM ? **OpenAI ????**??? Anthropic ??????? `image_url` ??

? Python ?? JSON?????????PowerShell `Invoke-RestMethod` ???

```powershell
# ?????? VISION_MODEL ???????
$VISION_MODEL = if ($env:VISION_MODEL) { $env:VISION_MODEL } else { 'moonshotai/kimi-k2.6' }

# Python ???? JSON ?????
python -c @"
import json
b64 = open(r'$env:TEMP\img.b64').read().strip()
mt = '$MT'
body = {
    'model': '$VISION_MODEL',
    'messages': [{
        'role': 'user',
        'content': [
            {'type': 'image_url', 'image_url': {'url': f'data:' + mt + ';base64,' + b64}},
            {'type': 'text', 'text': r'<????????>'}
        ]
    }]
}
json.dump(body, open(r'$env:TEMP\nvidia_req.json', 'w'), ensure_ascii=False)
"@

# ?? Nvidia NIM API?OpenAI ?????
$RESP = Invoke-RestMethod     -Uri 'https://integrate.api.nvidia.com/v1/chat/completions'     -Method Post     -Headers @{ Authorization = "Bearer $NVIDIA_KEY" }     -ContentType 'application/json'     -InFile "$env:TEMP\nvidia_req.json"     -TimeoutSec 90
```

### 3. ???????????

Nvidia NIM ???? OpenAI Chat Completions ???

```powershell
# ?????? choices[0].message.content
$TEXT = $RESP.choices[0].message.content
Write-Output $TEXT
```

??????? reasoning/thinking ?????? `choices[0].message.reasoning_content` ??????????????? `.content`?

### 4. ????????

????????????**???????**???????????"??"???????????????

## ????

?????????? content ?????? `image_url` ??

```python
'content': [
    {'type': 'image_url', 'image_url': {'url': f'data:image/png;base64,{b64_1}'}},
    {'type': 'image_url', 'image_url': {'url': f'data:image/png;base64,{b64_2}'}},
    {'type': 'text', 'text': '??????...'}
]
```

## ????

- **API ??**?Nvidia NIM ? OpenAI ?????? `image_url` ????? Anthropic ? `image`/`source` ??
- **API key ?????**????????? echo / Write-Host / print?
- **???????**?????? `$env:TEMP
vidia_req.json`??? `-InFile` ???????????????
- **key ??**?cc-switch ?? Nvidia key ??? `nvapi-...`??? `sk-...`?
- **????**?`-TimeoutSec 90` ???????????
- **media_type ???????????**???? jpg ? image/png?

## ????

| ?? | ?? | ?? |
|------|------|------|
| `401 Unauthorized` | API key ????? | ?? `$env:NVIDIA_API_KEY` ? cc-switch ? Nvidia provider ? token |
| `404 Not Found` | ?????? | ? [NVIDIA NIM](https://build.nvidia.com/) ??????????? |
| `400 Bad Request` | ?????? | ????? OpenAI ???`image_url` ????? Anthropic ???`image` ?? |
| ?? | ??? | ?? `-TimeoutSec` ?????? |
| ???? | key ??? header | ?? `Authorization: Bearer ` ?? key?key ??????? |

## ? Claude ?????

? skill ? `image-vision-qwen`?Claude Code ? DashScope Qwen ???? Codex ??????????????????????????

- **Claude ?**?? DashScope Anthropic ???? `image` ??Anthropic ???
- **Codex ?**?? Nvidia NIM OpenAI ???? `image_url` ??OpenAI ???

?? skill ?????? GitHub ?? [image-vision-qwen](https://github.com/wangyikun123/image-vision-qwen)?
